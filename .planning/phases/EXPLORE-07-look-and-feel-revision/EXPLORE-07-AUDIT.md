# EXPLORE-07 Look-and-Feel Audit Table (before fixes)

**Gathered:** 2026-09-24 · **Branch:** `phase-7` · **Baseline:** `efec1d5` (plan artefacts) · 200/200 tests green
**Protocol:** redesign-existing-projects SKILL.md — Scan → Diagnose → Fix. This table is the Diagnose artefact, committed BEFORE any fix commit (REV-10/D-01). Every later phase-7 commit references a row id (`A-NN`).
**Fix order** (skill priority, lines 159-169): typography/hierarchy → color → hover/active states → layout/spacing → components → polish. Row ids are referenced by the delivering commits.
**Mandatory regions covered (§8):** shell frame · header bar · intro strip · panel grid · About+Contact · Experience · Skills · Projects — plus disposition-only scans: status bar · drawer · tour overlay (rows A-16…A-20).

| Region | Area | Generic pattern / weak point (before) | Disposition | Fix locus | Test / no-op proof |
|---|---|---|---|---|---|
| A-01 | Panels (Experience) | Career-span Gantt chart = generic AI-dashboard pattern (user-disliked: "I don't like the gantt chart at all") | fixed | EXPLORE-07-01 task 3 — file + builder + section wiring + suite deleted | tests/explore-visuals-server.test.mjs composition contract (timeline `<ol>` first body child, no chart import); absence greps in explore-visuals.test.mjs |
| A-02 | Panels (Projects) | Year-grid calendar chart = same generic dashboard pattern (user-rejected) | fixed | EXPLORE-07-01 task 2 — file + builder + gate + suite deleted (tracer removal) | tests/explore-visuals.test.mjs calendar-export rewrite: absence + 6 card names; tests/projects-calendar.test.mjs dropped |
| A-03 | Experience | Stacked equal-weight duration/location lines (two muted paragraphs, no hierarchy) | fixed | EXPLORE-07-01 task 3 — merged meta row `<p>` with tabular-nums duration + aria-hidden ` · ` + location (U-8, UI-SPEC §3.1) | explore-visuals-server.test.mjs merged-meta-row pins (verbatim strings, dash style unnormalized) |
| A-04 | Skills | Competency cards: uniform chip+proof rows, no rhythm, no hover response (REV-09) | fixed | EXPLORE-07-02 — card restyle: exp-lift hover, alternating chip alignment (index parity), proof mt-2 (UI-SPEC §4) | explore-visuals-skills.test.mjs: chip/pointer preserved pins + export test: every competency name+proof renders verbatim |
| A-05 | Panel grid | Uniform gap-4 gutters at all tiers (flat rhythm at lg, DENSITY 4) | fixed | EXPLORE-07-02 — `gap-4` → `gap-4 lg:gap-5` on explore-panels.tsx grid (D-02) | explore-sweep.test.mjs grid greps still substring-match; explore-shell.test.mjs grid regex unchanged |
| A-06 | Panel headers | No hierarchy device beyond chip+label (flat header row, VARIANCE 5 gap) | fixed | EXPLORE-07-02 — oversized mono index 01–04 on the four PanelShell headers (§5, D-02; aria-hidden, ml-auto) | panel-shell anatomy greps (chip/label/slot intact, hover-inert); index computed `String(i+1).padStart(2,'0')` — no banned stat literals |
| A-07 | Projects (linked cards) | Color-only hover affordance (name+arrow accent shift, no physical response) | fixed | EXPLORE-07-03 — M2 lift+bloom: `exp-lift` 2px translateY + `--panel-shadow-hover` hsl-tinted bloom (§6.4) | globals.css greps: shadow tokens defined in both theme blocks; hover rule scoped under `.explore-shell` |
| A-08 | About+Contact | Contact rows: static icons, hover-only underline, no keyboard parity | fixed | EXPLORE-07-03 — M3 icon nudge (`exp-nudge` translateX) + M5 `group-focus-visible:underline` parity (§7) | globals.css greps (nudge keyframes-free transition under `.explore-shell`); Tailwind 3.4.19 compiles `group-focus-visible:` (RESEARCH §2) |
| A-09 | Panel grid (entrance) | No entrance choreography; panels mount at full opacity with no rhythm | fixed | EXPLORE-07-03 — the ONE entrance: `.panel-grid > *` stagger keyframes, 240ms + 40/80/120ms delays, `backwards` fill (§6.2, D-04) | greps: `@keyframes explore-panel-in` + nth-child delays + `panel-grid` class; all scoped under `.explore-shell` → reduced-motion guard covers |
| A-10 | Header bar | No pressed-state feedback on the 4 ghost controls (hover bg only, instant) | fixed | EXPLORE-07-03 — `active:bg-muted/80` press-settle on header controls (OQ-4/U-4) | explore-header.tsx source greps; 44px targets + focus rings unchanged (anatomy greps) |
| A-11 | Tour copy | Dangling "career-span chart" reference in the experience step body (U-10) | fixed | EXPLORE-07-01 task 3 — constants.ts:97 rewrite: "Roles in order — title, company, tenure, and the shape of the career as a timeline." | explore-tour.test.mjs source-grep: no `chart` substring in any step body; digit-guard loop stays green (no digits beyond "60") |
| A-12 | lucide icons everywhere | Skill flags lucide/Feather as the default AI icon choice | kept-by-design | none | Project already depends on lucide-react 0.475.0 (D-05: no new dependencies); skill's own override clause — work with the existing stack |
| A-13 | JetBrains Mono / tokens / PanelShell chrome | Skill would swap display fonts and restyle card chrome | kept-by-design | none | IDE aesthetic is the hard rail (D-05/SPEC): tokens, JetBrains Mono, `rounded-md border bg-card p-4` chrome, themes, drawer/header/status structure — tune WITHIN, never replace |
| A-14 | Focus rings / 44px targets / graceful-hide | Missing-state scan (skill §Interactivity) | kept-by-design | none | Already correct: §14 ring recipe on every anchor (greps pass), min-h-[44px] rows/buttons, graceful-hide with no invented copy (existing suite pins) |
| A-15 | Loading / error states | Skill's loading/error/empty-state checklist | no-op | none | Static export, no forms, no async data — nothing to load or fail (existing no-shimmer/no-humor tests hold) |
| A-16 | Status bar (disposition-only) | Stale prose comment: "N/5 sections visited" while code renders EXPLORE_SECTIONS.length = 4 | fixed | EXPLORE-07-01 task 3 — comment-only correction to N/4 (R11; no chrome replacement) | Comment stripped by `codeOf` convention; explore-tour.test.mjs:586 already asserts the live "0/4 sections visited" string |
| A-17 | Drawer (disposition-only) | Scanned: Radix Sheet chrome, 44px rows, accent digits | kept-by-design | none | Chrome replacement out of scope (UI-SPEC §8); drawer stays byte-identical (locked phase-1 pin) |
| A-18 | Tour overlay (disposition-only) | Scanned: in-flow fixed overlay, NOT portaled (W-6 pin); `tour-dim-in` keyframe inline | kept-by-design | none | Paint-order pin (W-4/W-6) — never portaled; inline keyframe pre-dates the phase and is guard-covered; no chrome change |
| A-19 | Shell frame | Scanned: h-dvh (not 100vh), single scroll container, full-bleed (no max-width wrapper) | kept-by-design | none | Existing pins are correct per the skill's own checks (dvh > vh, container via scroll-padding); max-width wrapper explicitly banned (explore-sweep greps) |
| A-20 | Intro strip | Scanned: typewriter strip with sr-only h1 + reduced-motion matchMedia swap | kept-by-design | none | U-2 resolved: typewriter-only, excluded from the stagger; CLI TypingEffect reuse protected (D-07); existing intro tests hold |

## Protocol notes (falsifiability)

- **Seeded rows 1–8:** validated as written; refined into A-01…A-11 with explicit fix loci per delivering plan/task. Seeded row 4 (gutters) and row 5 (index) split from the generic "panel grid" row into A-05/A-06 for per-commit referenceability.
- **Seeded rows 9–12:** validated and kept as A-12…A-15 verbatim (kept-by-design/no-op rows recorded per the skill's discipline).
- **Amendments beyond the seed:** A-10 (header press-settle, from U-4/OQ-4 — the audit's own protocol surfaced it), A-16 (stale N/5 comment — R11), A-09 (entrance row — REV-11 is a fix the audit must carry), A-17…A-20 (disposition-only scans for drawer/tour/status/shell/intro per §8's mandatory-region + scanned-surfaces contract).
- **Fix order compliance:** this phase's commits follow the skill priority — removals (charts) first, then hierarchy (index/gutters/skills), then motion states (lift/nudge/underline/active) — matching plan waves 01 → 02 → 03.
- **No unrelated refactors (REV-10):** every code change in this phase maps to exactly one row above; rows A-12…A-20 are explicit refusals, recorded so the absence of change is auditable.