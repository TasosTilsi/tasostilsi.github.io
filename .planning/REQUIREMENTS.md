# Requirements

## EXPLORE

- [x] EXPLORE-01: A new /explore route renders a techy-but-visual landing page with an IDE-like layout (sidebar, panels, consolas/mono typography, typewriter intro) that presents the portfolio content clearly.
- [x] EXPLORE-02: The page presents the work with visualizations — a skills chart, an experience timeline, and project stats — built with recharts from the existing portfolio data.
- [x] EXPLORE-03: The page has clear, visible navigation sections (About, Experience, Skills, Projects, Contact) that a non-technical visitor can scan in ~30 seconds without typing any commands.
- [x] EXPLORE-04: The page includes light gamification — a guided 'spin the wheel' tour picker, exploration progress/achievements, and light humor in the copy.
- [x] EXPLORE-05: The existing CLI terminal (/) and printable resume (/resume) remain fully intact, and a non-blocking toggle/link routes visitors between the CLI and /explore.
- [x] EXPLORE-06: The /explore page is responsive and works well on mobile, since recruiters are often on phones.
- [x] EXPLORE-07: All content on /explore is data-driven from src/data/portfolio-main-data.json, with no hardcoded portfolio content.

## REV

- [x] REV-01: The portfolio data file is refreshed to the user's current resume (docx) as source of truth — new branding ("Test Automation Architect | Principal Test Automation Engineer"), business-value summary, 8 core-competency clusters with proof lines, quantified experience bullets, enriched Clarif-AI/DeepIndex entries, featured-flagged certifications, and selected-writing curation — flowing consistently to ALL surfaces (CLI, /explore, /resume, PDF export), with nothing deleted (full lists stay in the CLI).
- [x] REV-02: The /resume page is rebuilt to the docx structure — SUMMARY → CORE COMPETENCIES → PROFESSIONAL EXPERIENCE (quantified bullets) → PROJECTS → EDUCATION → CERTIFICATIONS → SELECTED WRITING — print-optimized.
- [x] REV-03: The static PDF export (scripts/generate-static-resume.js → public/resume-export.html) is rebuilt to the same docx structure, replacing its hardcoded sections.
- [x] REV-04: The About and Contact panels merge into one panel on /explore, reclaiming the empty space the oversized About section left, with the panel grid rebalanced.
- [x] REV-05: The Skills panel's bar chart and treemap are replaced by competency + proof cards — 8 core-competency clusters, each with a one-line quantified proof drawn from the refreshed data.
- [x] REV-06: The Projects panel gains a year-grid bars calendar (year columns, project bars spanning their active months) alongside the existing cards.
- [x] REV-07: The Experience showcase is redesigned per the user's reference screenshot (layout description pending from the user — this requirement is scoped when the description arrives; the current text timeline + Gantt remain until then). **[SCOPED 2026-09-22 — user's full semicircular-timeline brief; delivered as phase 8, see REV-12/REV-13 for the interaction contracts]**
- [x] REV-08: Both Gantt-style charts are removed from /explore — the career-span chart in the Experience panel and the year-grid bars calendar in the Projects panel — with the Experience showcase redesigned as a polished non-chart presentation and Projects reverting to tiles + cards; stale tests updated/removed per the replacement discipline.
- [x] REV-09: The Skills panel's competency cards are redesigned in presentation — spacing, hierarchy, accent rhythm, and restrained motion — under the design-taste-frontend + motion-design guidance, with the IDE aesthetic (tokens, JetBrains Mono, panel chrome) preserved.
- [x] REV-10: A full audit-first look-and-feel pass across /explore (shell, header, intro, all panels) applies the redesign-existing-projects sequence: audit current design, identify generic patterns, fix spacing/hierarchy/typography/accent consistency.
- [x] REV-11: A restrained micro-motion vocabulary (CSS-only hover/focus transitions plus one entrance choreography, auto-suppressed under prefers-reduced-motion by the phase-1 guard) is applied per the motion-design skill.
- [x] REV-12: The Experience panel becomes a full-width interactive semicircular career timeline (REV-07 delivered): a mathematically positioned arc (x = centerX + radius·cos(angle), y = centerY + radius·sin(angle)) carries year markers for the 3 docx tech roles (Chubb, Upstream Systems, Netcompany-Intrasoft — the non-tech roles stay CLI-reachable); the ACTIVE role sits at the arc's focal point with the role content (large title, company, dates, location, quantified achievement bullets) rendered right of the arc; scrolling inside the section drives timelineProgress (0→1), a single source of truth from which arc positions, active index, marker emphasis, and content transitions all derive; reaching the arc ends releases normal scrolling; the panel spans the full grid width (both columns at md+) with the grid rebalanced.
- [x] REV-13: The interaction-quality contract for the timeline: hand-rolled rAF interpolation (zero new animation dependencies); keyboard users navigate roles via arrow keys/buttons with an accessible non-scroll alternative; prefers-reduced-motion replaces spatial transitions with simple opacity/state transitions; listeners/observers cleaned up on unmount; mobile collapses to a simplified compact vertical/curved timeline preserving the same visual language; all content data-driven from the refreshed portfolio-main-data.json experience entries (EXPLORE-07 survives).
- [x] REV-14: The panel order reflows — [About+Contact | Skills] becomes row 1 (About first as requested), Experience full-width row 2, Projects row 3 — with the drawer, wizard steps, and visited counter re-mapped to the new DOM order.
- [x] REV-15: The About+Contact panel is enhanced: a two-line positioning-statement lead above the (demoted, smaller) full summary; an impact-metric row (12+ engineering teams · 30+ engineers · 7+ years · npm launch week) derived from the competency-proof data; an availability badge ('Open to selective part-time work' — one new data field transcribed from the user's own CLI banner line); the small rounded avatar from about.profileImageUrl; all 9 contact rows kept (tightened).
- [x] REV-16: The Experience arc merges education: the semicircular timeline carries 5 entries chronologically — BEng (2012), Netcompany (2019), MSc (2021), Upstream (2022), Chubb (2023) — with type-aware content templates (roles: title > company > dates > location > quantified bullets; education: degree > institution > dates > specialization), the education entries being the 2 featured education records from phase 6; the stage keeps its hand-rolled rAF engine (no framer-motion in the Experience panel).
- [x] REV-17: The Projects panel gains a framer-motion-driven editorial scroll composition — the top-6 featured/curated projects as rows (year + name + one-line description + link, thin horizontal dividers) that enter from below and coexist during transitions inside a sticky ~100vh stage, continuous scroll-linked interpolation derived from a single progress value, Editorial-calm motion character (no bounce/springs); framer-motion (already a dependency, 39th) is the engine for this composition ONLY — the Experience stage keeps its hand-rolled rAF.
- [x] REV-18: The Projects panel's editorial scroll is replaced by a curated stacked-card carousel: the top-6 projects rendered as physically stacked overlapping cards in depth (card appearance = f(cardIndex, carouselProgress) — translateY/scale/opacity/z-index derived continuously, no discrete state swaps) inside a sticky ~100vh scroll-driven stage; the foreground card moves away and the next card promotes as the user scrolls, both directions reversible.
- [x] REV-19: Each card is visual-first: rounded corners, a generative IDE-language visual (monochrome per-project composition — terminal mocks, architecture glyphs, report dashboards — deterministic per project, no image assets), a compact header (project name + tagline), 75-85% visual area; curated imperfection via deterministic per-index offsets (±2-4px, ±1° rotation); in-card info: the ACTIVE card's header expands to carry description, technologies, and links.
- [x] REV-20: The interaction-quality contract for the stack: keyboard Prev/Next controls with an accessible non-scroll alternative; prefers-reduced-motion replaces physical movement with state transitions; listeners cleaned up; mobile collapses to a simplified stacked composition (active card dominant, readable content); all content data-driven from portfolio-main-data.json (EXPLORE-07).
- [ ] REV-21: A combined 'Credentials' panel sits beside the Projects stack (row 3: [Projects stack | Credentials]) with 3 tabs — Articles (5 featured), Certifications (5 featured: ISTQB + Anthropic), Presentations (1: Allure Reporting, newly featured-flagged) — each tab listing its curated items as compact rows (title + date + link, calm presentation, no carousel machinery); the drawer and visited counter grow to 5 sections (N/5); the wizard step sequence stays as-is.
